'use strict';

const printOptions = {
  trailingComma: true,
};

describe('rm-merge', () => {

  it('transforms correctly', () => {
    test('rm-merge', 'rm-merge-test', {
      printOptions,
    });
  });

});
