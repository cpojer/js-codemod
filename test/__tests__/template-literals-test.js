'use strict';

const printOptions = {
  quote: 'single',
};

describe('template-literals', () => {

  it('transforms correctly', () => {
    test('template-literals', 'template-literals-test', {
      printOptions,
    });
  });

});
